#include "pch.h"
#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	float a;
	float b;
	cin >> a;
	cin >> b;
	cout << abs(a) + abs(b);
	cout << endl;
	cout << abs(a) - abs(b);
	cout << endl;
	cout << abs(a) * abs(b);
	cout << endl;
	cout << abs (a) / abs(b);
}
