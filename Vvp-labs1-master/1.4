#include "pch.h"
#include <iostream>
using namespace std;
int main()
{
	float a;
	float b;
	cin >> a;
	cin >> b;
	cout << ( a * a ) + ( b * b );
	cout << endl;
	cout << ( a * a ) - ( b * b );
	cout << endl;
	cout << ( a * a ) * ( b * b );
	cout << endl;
	cout << ( a * a ) / ( b * b );
}
